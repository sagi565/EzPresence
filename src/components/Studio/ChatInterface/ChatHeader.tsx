import React from 'react';
import { styles } from './styles';

const ChatHeader: React.FC = () => {
  return (
    <div style={styles.chatHeader}>
      <h2>Let's make a video</h2>
    </div>
  );
};

export default ChatHeader;