import styled from 'styled-components';
import { theme } from '@/theme/theme';

export const StyledChipArrow = styled.span`
  font-size: 10px;
  color: ${theme.colors.muted};
  margin-left: 10px;
  flex-shrink: 0;
`;
